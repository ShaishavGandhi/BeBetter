language: android
android:
        components:
                - tools
                - platform-tools
                - build-tools-27.0.3
                - android-27
                - extra-android-support
                - extra-android-m2repository
                - extra-google-m2repository

jdk:
        - oraclejdk8

script:
        - ./gradlew build
before_install:
        - chmod +x gradlew
notifications:
        email: false

sudo: false

cache:
  directories:
    - $HOME/.gradle